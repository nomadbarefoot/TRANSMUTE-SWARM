"""
Sorting solution — branch 'sort' owns this file only.
Improved: using Python's built-in sorted() for O(n log n) performance.
Contract: must define sort(arr) that sorts the list and returns it.
"""

def sort(arr: list) -> list:
    return sorted(arr)
