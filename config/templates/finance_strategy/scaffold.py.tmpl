def compute_signal(prices: list[float]) -> list[float]:
    """
    Given a list of daily prices, return a signal list of the same length.
    signal[i] > 0 means BUY, signal[i] < 0 means SELL, 0 means HOLD.
    Signals must be causal (no lookahead).

    Baseline implementation — optimize this.
    """
    raise NotImplementedError("Replace with your implementation")
