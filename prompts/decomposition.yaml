# Hand-written PoC decomposition. No transmuter agent yet.
run_tag: finance_poc_001
problem: "Optimize MA crossover strategy on NIFTY50 3-month daily data"

composite_metric:
  oracle: "python3 evaluate_composite.py"
  direction: lower_is_better

branches:
  - id: finance
    owns: [solutions/finance_ma.py]
    metric:
      name: finance_sharpe_neg
      oracle: "python3 oracles/evaluate_finance.py --mode full"
      direction: lower_is_better

composite_weights:
  finance: 1.0
