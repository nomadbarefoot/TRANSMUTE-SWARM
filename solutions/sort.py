"""
Sorting solution — branch 'sort' owns this file only.
Experiment: using Python's built-in sorted for O(n log n) performance.
"""

def sort(arr: list) -> list:
    """Use Python's built-in sorted function (Timsort)."""
    return sorted(arr)
